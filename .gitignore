node_modules/
.next/
out/
build/
.DS_Store
*.log
*.zip

# env files
.env
.env.local
# Any copy of one, however it was named. A backup of an env file is
# still an env file, and `git add -A` does not know the difference.
.env*.bak*
.env.local.*
.env.provisioning

# Provisioning state: database passwords and service-role keys for
# instances this machine created. Never committed.
.provisioning-state/
.env.*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Sentry Config File
.env.sentry-build-plugin
.claude/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
.next-e2e-unresolved/
